<Window x:Class="Circus.CircusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Circus"
        mc:Ignorable="d"
        Title="Цирк" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="tbTitle" Grid.ColumnSpan="2"
                       VerticalAlignment="Center" FontSize="30"
                       HorizontalAlignment="Center"/>

            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <Button x:Name="btnBack" Content="Назад" Margin="5 10" Click="btnBack_Click"/>
                <Button x:Name="btnForward" Content="Вперед" Margin="5 10" Click="btnForward_Click"/>
            </StackPanel>
        </Grid>

        <Frame x:Name="frameNav" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
